<?php
/**
 * @var string $message
 * @var \Sample\View $view
 */
?>
<div class="page-header">
    <h1><?= $view->e($title ?? 'Error') ?></h1>
    <div class="subtitle">Something went wrong handling this request.</div>
</div>

<div class="page-body">
    <section class="dashboard-section">
        <p style="color: var(--error-color)"><?= $view->e($message) ?></p>
        <p class="muted">If this is the first run: copy <code>.env.example</code> to <code>.env</code> and fill in <code>LOGDB_API_KEY</code>.</p>
        <div class="form-actions">
            <a href="/" class="btn btn-default">← back to dashboard</a>
        </div>
    </section>
</div>
